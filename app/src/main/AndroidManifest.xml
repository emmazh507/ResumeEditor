<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mjsearch.emma.resumeeditor">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.mjsearch.emma.resumeeditor.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--test share
        <activity
            android:name="com.mjsearch.emma.resumeeditor.EditBasicInfoActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/emma" />
            </intent-filter>
        </activity> -->
        <activity android:name="com.mjsearch.emma.resumeeditor.EditBasicInfoActivity"/>
        <activity android:name="com.mjsearch.emma.resumeeditor.EditEducationActivity"/>
        <activity android:name="com.mjsearch.emma.resumeeditor.EditProjectActivity"/>
        <activity android:name="com.mjsearch.emma.resumeeditor.EditExperienceActivity"/>

    </application>

</manifest>